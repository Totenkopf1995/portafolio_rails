<div class="container grid grid-cols-2 mx-auto py-5">
  <div class="text-white">
    <% anio = Time.now.year %>
    <%= "© Andres Torres 2023~#{anio}. Todos los derechos reservados" %>
  </div>
  <div class="text-right text-white">
    <p>Última modificación del proyecto: <%= `git log -1 --date=format:"%Y-%m-%d %H:%M:%S" --format="%cd"`.chomp %></p>
    <% actual= Time.now.strftime("%d-%m-%Y") %>
    <%= "Fecha actual: #{actual}." %>
  </div>
</div>